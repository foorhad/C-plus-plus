#include <bits/stdc++.h>
using namespace std;

int main()
{
    // function of at() function
    string s;
    cin >> s;
    // cout << s[0] << endl;
    cout << s.at(0) << endl; // s[0] and s.at[0] both are same

    // function of back() and front() function
    // cout << s[s.size() - 1] << endl; // manually
    // cout << s.back() << endl;        // builting function
    // cout << s.front() << endl;

    return 0;
}